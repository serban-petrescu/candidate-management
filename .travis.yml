matrix:
  include:
    language: java
    jdk: oraclejdk8
    script:
      - mvn clean install -X
    language: node_js
    install:
      - npm install newman
    script:
      - newman run cm-server/src/test/resources/postman/candidate-management.postman_collection.json -e cm-server/src/test/resources/postman/tests.postman_collection.json
cache:
  directories:
  - "$HOME/.m2"
  - node_modules
  - node
env:
  - CI=""
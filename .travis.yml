matrix:
  include:
    language: java
    jdk: oraclejdk8
    language: node_js
    node_js: "v8"
    before_install:
      - mvn clean install -X
    install:
      - npm install -g newman
    script:
      - newman run cm-server/src/test/resources/postman/tests.postman_collection.json
cache:
  directories:
  - "$HOME/.m2"
  - node_modules
  - node
env:
  - CI=""
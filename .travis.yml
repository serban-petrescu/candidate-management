matrix:
  include:
    language: java
    jdk: oraclejdk8
    language: node_js
    before_script:
      - npm install -g newman
      - mvn clean install -X
    script:
      - newman run cm-server/src/test/resources/postman/tests.postman_collection.json
cache:
  directories:
  - "$HOME/.m2"
  - node_modules
  - node
env:
  - CI=""